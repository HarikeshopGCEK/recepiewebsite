export default function MyRecipiesLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
export const metadata = {
    title: "My Recipies",
    description: "My Recipies",
};